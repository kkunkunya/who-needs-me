[package]
name = "who-needs-me-core"
version = "0.1.0"
edition = "2024"

[lib]
name = "who_needs_me_core"
path = "src/lib.rs"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["std"] }
serde_json = "1"
